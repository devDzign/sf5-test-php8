{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 d-flex flex-row justify-content-center">

                <div class="card mt-5 shadow-lg bg-white rounded border-primary" style="width: 22rem">
                    <h5 class="card-header text-center text-white bg-primary mb-3">Registration</h5>
                    <div class="card-body">
                        {{ form_start(form) }}
                        {{ form_errors(form) }}
                        {{ form_rest(form) }}
                        <button class="btn btn-primary">S'inscrire</button>
                        {{ form_end(form) }}
                    </div>
                </div>

            </div>
        </div>
    </div>

{% endblock %}